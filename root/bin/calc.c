#include <stdio.h>
#include <math.h> 
int check(int x){
  if(x == int

int main(int argc, char *argv[])
{
  switch(argc)
  {
     case 4;
     int num[] = {argv[2], argv[4]};
     char opr = argv[3];
     break;
  }

  switch(opr){
     case "+";
     printf("%d"
    
       
     
}
